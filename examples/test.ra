model User {
	id        Int     @id;
	firstName String;
	lastName  String;
	tasks     Task[];
}

model Task {
	id     Int     @id;
	title  String;
	isDone Boolean @default(false);
	user   User    @relation("taskUser", [userId], [User]);
	userId Int     @unique;
}
